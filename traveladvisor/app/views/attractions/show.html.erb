<h2><%= @attraction.name %> <%= @attraction.address %></h2>
<% @attraction.reviews.each do |review| %>
  <%= review.author %> - <%= link_to review.title, attraction_review_path(@attraction, review.id) %>
  <%= review.content %>
<% end %>
